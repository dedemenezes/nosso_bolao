
<div class="container">
  <div class="d-flex px-4 py-2 justify-content-between align-items-center">
    <h1 class='text-center'><%= @matches.first.tournament.name %></h1>
    <%#= render 'participants/new' %>
  </div>
  <%= render 'shared/sweepstake_navigation', tournament: @tournament %>
  <% @matches.each do |match| %>
    <%= render 'matches/index/match', match: match %>
  <% end %>
</div>
